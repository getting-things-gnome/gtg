# Configuration for CircleCI https://circleci.com

machine:
  python:
    version: 3.4.2

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install -qq python3-gi gir1.2-gtk-3.0 gir1.2-networkmanager-1.0
    - pip install -U "setuptools>=17.1"
